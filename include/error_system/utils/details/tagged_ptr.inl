#pragma once

/**
 * @file tagged_ptr.inl
 * @brief tagged_ptr_t 模板实现
 * @details tagged_ptr_t 的所有方法均为短小内联函数，已在头文件中定义。
 *          本文件为占位文件，保持与项目其他模板（.h + .inl）一致的文件组织，
 *          由 tagged_ptr.h 在末尾 #include。若后续需要添加非内联的模板特化
 *          或复杂实现，应放在此文件中。
 * @author yiice
 * @version 1.0.0
 * @date 2026-07-07
 * @copyright Copyright (c) 2026
 */
namespace error_system::utils {

    // tagged_ptr_t 的所有方法已在头文件中内联实现。
    // 此命名空间块用于保持文件完整性，便于后续扩展。

}  // namespace error_system::utils
